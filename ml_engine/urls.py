from django.urls import path, include
from rest_framework.routers import DefaultRouter

app_name = 'ml_engine'

urlpatterns = [
    # ML engine endpoints will be added here
    # For now, keeping it simple
]
